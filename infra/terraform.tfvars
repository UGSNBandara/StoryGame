# This file contains actual values for variables
# Change these values for your own deployment

# AWS Configuration
aws_region = "eu-north-1"  # Stockholm region (eu-north-1 = Sweden)

# EC2 Configuration
instance_type = "t3.micro"  # Small server size (free tier)

# SSH Key (must exist in AWS)
key_name = "storygame-key"  # Name of your SSH key in AWS console
